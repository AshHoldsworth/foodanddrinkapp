export type Consumable = {
    name: string
    ingredients: string[]
    rating: number
    isHealthyOption: boolean
    cost: number
    type: string
    difficulty: number
    speed: number
    course: string
}
